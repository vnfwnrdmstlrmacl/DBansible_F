# Generated by Ansible
# Export: {{ exports_export_path | default(backup_nfs_export_dir) }}

{#
  exports_export_path : export directory path
  exports_clients     : list of client IP addresses
  (둘 다 nfs_server.yml에서 vars로 넘긴다. 호환을 위해 기존 변수도 fallback 한다.)
#}

{% set export_path = exports_export_path | default(backup_nfs_export_dir) %}
{% set clients = exports_clients | default(backup_client_addrs | default([])) %}

{{ export_path }} {% for ip in clients -%}{{ ip }}(rw,sync,no_root_squash,no_subtree_check){% if not loop.last %} {% endif %}{% endfor -%}

